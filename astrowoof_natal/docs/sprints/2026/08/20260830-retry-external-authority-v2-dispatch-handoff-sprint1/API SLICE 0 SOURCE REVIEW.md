# API review — Slice 0 source characterization

## Disposition

The source/public-contract assessment is approved as a provisional causal
classification. It correctly establishes that SBE `0.4.30` already has a safe,
complete provider-free post-fan-in path and that generic dispatch refusal itself
does not indicate a missing native create primitive.

The most useful current hypothesis is therefore an API consumer/routing defect:
API must consume the selected local-only work first, re-inspect, then construct
and use the fresh exact v2 request/grant/document handoff. An API admission row
must never be attached to generic ordinary resume as create authority.

## Retained-evidence caution

The attached API coordinate packet names the exact active protected archive for
each run and the later persisted lifecycle-basis record relevant to the incident.
It is a coordinate/join packet, not a claim that the archive already proves the
lifecycle snapshot association. During exact GET inspection, validate archive
inventory, manifest/member hashes, native snapshot identity, and the timeline
before treating that association as causal evidence.

In particular, do not project the dashboard's completed Responses into either
archive unless native result/receipt members establish their retrieval and
consumption.

## Approved next work

SBE may use the packet's two exact keys for the already-approved one HEAD and
one GET per object, following the plan's archive-safety and zero-mutation
protocol. Then complete the two retained timelines and classify the earliest
public API/SBE handoff boundary. No recovery, provider access, worker resume,
deployment, or release is authorized by this review.
