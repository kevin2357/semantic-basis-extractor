# Slice 5 — final release-candidate review

## Recommendation

SBE `0.4.30` is ready for final API/owner release review. Tagging and publication
remain unauthorized until that explicit gate.

## Qualified correction

- Applicable exact-interactive ordinary paid actions cannot use generic
  spend-authorization resume to cross provider create. The public exit-0 refusal
  requires fresh lifecycle inspection and constrained v2 dispatch.
- A local semantic-progress contradiction seals a terminal-review v0.2 result,
  canonical receipt, and invocation-bound exit-2 envelope before command exit.
- Provider-bearing custody is retained and new provider creation is false.

## Evidence

- Full suite: 890 passed, 46 expected skips.
- Two deterministic wheels are byte-identical at SHA-256
  `19a8728b35281e2415ec0b407ef882a505576e41c81d34488961ce08b5a83e9a`.
- Generic installed smoke, duplicate-submission-fence qualification, and
  terminal-review qualification all pass from real `site-packages`.
- Zero real provider calls/retrievals/spend and zero retained-QA/R2 access after
  the bounded Slice 0 investigation.

## Consumer condition

API must strictly consume `astrowoof.generic_provider_dispatch_refusal.v1` and
route it to fresh lifecycle inspection/v2 dispatch. It must not interpret exit
zero as generic-resume success or retry. The terminal contradiction continues
through existing exact native-result ingress as `review_required` with provider
custody retained.

No historical Marmalade recovery, duplicate-response canonical selection,
worker resume, deployment, or paid QA is included in this release gate.
