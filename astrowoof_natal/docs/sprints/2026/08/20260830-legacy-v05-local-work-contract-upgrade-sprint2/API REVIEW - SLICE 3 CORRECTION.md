# API review — Slice 3 correction

## Decision

Approved for Slice 4's lean installed-wheel qualification and release gate.

The required correction is present:

- the bundle validator now derives the frozen v0.5 semantic seam from the
  embedded public inspection's exact ordinary-resume/capacity posture, empty
  `local_dependencies`, and completed-provider-evidence custody;
- it no longer treats the `legacy_predicate_failures` summary as sufficient
  proof;
- rehashed mutations cover the seam relation, local-source projection, retained
  custody projection, and retry-lineage conflict projection; and
- SBE's v0.5/v0.7/v0.8 validators remain canonical for document composition.

The additive receipt remains qualification-only and no runtime lifecycle,
provider, custody, authority, or mutation behavior has entered scope.

## Release conditions

Proceed only under the companion plan's lean-patch gate: fresh version before
candidate tests, exact packaged-resource audit, clean-installed wheel,
network-free double qualification with byte-identical receipts, deterministic
double build, `pip check`, scope audit, and `git diff --check`. A full runtime
suite is not required if—and only if—the final scope audit still proves no
production runtime change.

Pause for final API/owner release review before commit, tag, or publication.
