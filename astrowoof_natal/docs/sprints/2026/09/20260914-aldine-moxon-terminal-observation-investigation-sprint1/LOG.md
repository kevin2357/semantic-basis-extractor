# Log

- 2026-09-14: Sprint opened from the fresh QA cohort. No retained-workspace mutation, provider activity, or runtime implementation is authorized by this opening record.
- 2026-09-14: Validated the supplied export before interpreting it. Render's
  1,000-record cap truncated the requested two-hour window at
  `19:08:05.978Z`; it contains neither Aldine nor Moxon. Corrected the evidence
  premise and expanded the plan around narrowly filtered/segmented logs,
  separate fast-terminal and observer analyses, and an explicit post-Sprint-97
  workspace-root carry-forward check.
- 2026-09-14: Validated the eight replacement unfiltered segments and completed
  Slice 0. Aldine and Moxon have complete target-bound coverage through
  closeout. Aldine's apparent fast terminal is explained by six initial
  provider results plus two rejected polish attempts; no premature native
  closure is evident. Both exact terminal results still reach the observer and
  fail locally as `capture_or_preflight`, leaving the shared observer identity/
  phase join as the active investigation.
- 2026-09-14: API rejected filtered exports as completeness evidence because
  server-side filtering has previously omitted events. It replaced them with
  the original two-hour request split into eight unfiltered, non-overlapping
  15-minute UTC windows. Manifest and digests are recorded in `EVIDENCE.md`.
- 2026-09-14: Completed Slice 2 to the retained-evidence boundary. Exact result
  carry-forward, deployed SBE `0.4.61`, the post-Sprint-97 API root correction,
  and stable per-run native root digests are established. Current events omit
  the other live root digests and collapse all local capture/preflight
  exceptions, so no narrower failing phase is supportable without the gated
  exact-workspace reproduction. No R2, provider, retry, or mutation occurred.
- 2026-09-14: Owner authorized and SBE completed exactly one HEAD plus one
  ETag-conditional bounded GET for each pinned generation-9 checkpoint. Both
  archives and inventory manifests matched. Network-disabled, read-only exact-
  root reproduction found Moxon's full capture and both API deterministic
  preflights successful; Aldine produced a valid transportable
  `contradictory_native_evidence` status. The shared live failure is now bounded
  to API's post/runtime region or a deployed runtime/configuration difference.
  Stopped for joint review without providers, live writes, or mutation.
- 2026-09-14: API approved Slice 3 and accepted ownership of the remaining live
  observer/runtime defect. Closed the SBE investigation with no implementation,
  version bump, release, further R2 access, provider activity, or live-run
  action required.
