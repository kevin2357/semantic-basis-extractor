# Log — Bembo/Morris runtime-capture TypeError investigation

- Froze the two complete worker-log exports, witness identities, matching
  call-time/logical-root digests, and immutable SBE/API release provenance.
- Mapped the public capture function's pre-assembly collector escape, guarded
  assembly behavior, and possible typed-status double-fault. Eleven ordinary
  delivery/review controls passed.
- Received joint Gate A approval and two immutable API coordinate packets.
- Owner directly authorized exactly one conditional HEAD and one bounded GET
  for each named checkpoint. Both access budgets were consumed without listing,
  retry, write, delete, provider work, or state mutation.
- Verified both archives, all 1,850 workspace members, and both canonical
  inventory digests before extraction.
- Reproduced each exact result at its original logical root in disposable,
  network-disabled, read-only containers. Bembo returned a delivery packet at
  every public layer. Morris returned typed
  `unsupported / contradictory_native_evidence` at evidence collection and
  both capture exports. No stage raised.
- Removed all completed disposable probe containers. Retained verified local
  archives and extracted copies under `C:\tmp`; no further R2 access remains.
- Closed at Review Gate B with no SBE semantic change or release. Follow-up
  native phase/frame observability is separated into its own reviewed sprint.
