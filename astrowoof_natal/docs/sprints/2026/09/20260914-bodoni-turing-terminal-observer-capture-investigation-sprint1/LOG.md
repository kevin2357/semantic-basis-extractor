# Log

- 2026-09-14: Opened the investigation after Bodoni and Turing selected exact
  terminal-review authority but produced no Better Stack editorial packet or
  artifact POST.
- 2026-09-14: Detected that the original file 07 export reached Render's
  1,000-line cap. API supplied three unfiltered five-minute replacements.
  Hash-pinned Slice 0 chronology proved both witnesses reached capture entry
  and returned `unavailable` without envelope, preflight, or HTTP activity.
- 2026-09-14: Completed provider-free public-boundary mapping. API's installed
  SBE 0.4.61 export is the exact runtime implementation. The evidence collector
  precedes packet assembly's typed-status guard, but the caught live exception
  remained unclassified.
- 2026-09-14: Reproduced the API telemetry defect: CamelCase Python exception
  names violate the lowercase reason-token contract. The best-effort emitter
  silently drops both the failed capture phase and the later completed outcome.
- 2026-09-14: Owner authorized exactly one conditional HEAD and one bounded GET
  per coordinate-pinned witness. All four operations succeeded; archive sizes,
  ETags, archive hashes, inventory hashes, and every workspace member verified.
  The read budget is consumed.
- 2026-09-14: Network-disabled, read-only exact-root reproduction returned
  `unsupported / contradictory_native_evidence` for both retained checkpoints.
  Neither workspace reproduced the live exception. Release-tag source, current
  source, and API's installed relevant 0.4.61 modules are equal.
- 2026-09-14: API accepted ownership of the remaining observability correction:
  normalized exception tokens, a digest-only mirrored unavailable warning, and
  production-emitter regressions for `ValueError`, `KeyError`, and typed
  `unsupported`. Closed SBE work with no runtime change, version bump, release,
  Alloy revision, further R2 access, provider activity, or live-state mutation.
