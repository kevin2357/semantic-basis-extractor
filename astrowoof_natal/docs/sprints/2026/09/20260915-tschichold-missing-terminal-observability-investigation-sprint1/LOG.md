# Log

## 2026-09-15 — Sprint opened

- API completed a post-0.4.64 Better Stack audit using both the hot and archived
  log collections, then compared returned rows against the authoritative QA
  cohort membership.
- Tschichold was the only completed, non-quarantined post-deploy run without a
  full editorial packet and longitudinal artifacts.
- API supplied a hash-bound latest checkpoint coordinate packet and the exact
  native/result/receipt identities for bounded read-only investigation.
- No R2, provider, API lifecycle, retained-workspace, or Better Stack mutation
  occurred while opening this sprint.

## 2026-09-15 — Exact trace classification

- Better Stack showed exact v0.2 terminal-result publication and successful
  review-authority selection for `nres_baa5f597cf71ed0be89dd2a8`.
- The wrapper entered capture; root normalization, exact-result reading,
  eligibility classification, and exact-source proof all completed.
- Capture then raised `ValueError` at
  `editorial_review_runtime.py:428`, phase
  `pre_assembly_evidence_collection`. No packet assembly or repository POST was
  attempted.

## 2026-09-15 — Authorized retained-evidence proof and disposition

- The owner separately authorized exactly one conditional HEAD and one bounded
  GET for checkpoint key
  `v1/checkpoint/27e5815cb2af4200a1bb45aed25ae17b`.
- The access used one HEAD and one ETag-bound ranged GET, with zero listings,
  writes, deletes, provider operations, or lifecycle mutations.
- The retrieved archive matched 5,470,437 bytes and SHA-256
  `aabaebd72ff1cf52a02b77419845367e880c05369242d514b34b18954217747f`.
- Read-only replay at the original logical root reproduced the exact
  `Pass attempt evidence is incomplete` exception at line 428.
- Pass 3 attempt 1 is legitimately `ATTEMPT_ERROR` after an
  `IncompleteAuthoringDelivery`; it retains `openai-authored-fields.json` but
  has no `authoring-pass-acceptance.json` because acceptance QA never ran.
  Attempt 2 is the accepted creative retry and has both artifacts.
- The owner accepted this as a best-effort observability limitation. No code,
  schema, fixture, package, deployment, or run retry is authorized or needed.
