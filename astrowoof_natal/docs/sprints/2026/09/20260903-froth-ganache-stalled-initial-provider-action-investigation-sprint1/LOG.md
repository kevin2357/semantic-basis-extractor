# Log

- 2026-09-03: Sprint created from frozen Froth/Ganache QA evidence; SBE worker is suspended.
- 2026-09-03: Read the supplied worker trace only. It distinguishes two
  conditions: Froth published ordinary release-until-due provider custody;
  Ganache repeatedly reached final assembly after its sixth response completed,
  then raised an unsealed `AssemblyContractError` for a stale theme-group
  chapter reference. No retained workspace, R2 object, provider, API record,
  or native run was touched.
- 2026-09-03: Completed source mapping. The dormant theme-group change does
  not cover assembly's historical assignment-file join. More importantly, the
  current `finalization_contract_invalid` seal is implemented in ordinary
  authoring but not the reconciliation helper exercised by Ganache. Frozen the
  distinction as a candidate source defect pending exact checkpoint/result
  joins; Froth remains unclassified rather than assumed to share the cause.
- 2026-09-03: Per the API-created coordinate packet, performed exactly one
  HEAD and one ETag-conditional GET for each named checkpoint. Both archives
  matched their frozen byte and SHA-256 identities. Offline archive validation
  confirmed declared paths, member count/sizes/hashes, and aggregate byte
  count; no provider, worker, or retained workspace action ran.
- 2026-09-03: Froth generation 7 is an internally coherent five-REPORTED /
  one-WAITING initial wave. Its one provider identity had a recorded pending
  reconciliation outcome and future `resume_not_before`; its latest sealed
  result is `provider_pending`. Ganache generation 6 is likewise the
  pre-completion five-REPORTED / one-WAITING checkpoint, with no terminal
  review result. That frozen pre-completion state combined with the later trace
  establishes the suspected checkpoint-publication seam as a source-consistent
  candidate rather than a provider or API-custody ambiguity.
- 2026-09-03: Owner dashboard inspection confirmed Froth's exact durable
  response later completed. Its final SBE retrieval was `in_progress` at
  15:12:11 MDT, with reconciliation due again at 15:16:06 MDT; the worker trace
  has no later Froth invocation despite covering the subsequent period. Record
  Froth as a post-release API scheduling/dispatch investigation, not as a
  provider delay or an SBE re-submission defect.
- 2026-09-03: Slice 1 froze the Ganache bridge as reuse of the existing
  invocation-bound terminal-review v0.2 result, receipt, and command-result
  envelope: only a deterministic `AssemblyContractError` after exact result
  adoption and final custody closure may seal `review_required /
  finalization_contract_invalid`. Froth remains out of SBE runtime scope.
- 2026-09-03: Ran the focused terminal-review/native-transition baseline before
  Slice 2 source work: 32 passed, 2 expected optional-schema skips, and 1
  existing expectation mismatch. The mismatch expects `review_required` for a
  later mixed-custody successor; the current precedence reducer reports
  `provider_pending`, which is the custody-first posture. No source change was
  made before recording it.
- 2026-09-03: API approved Slice 2. Froth's missed post-due invocation and
  Ganache's retained attempt-64 capacity allocation are API scheduling/capacity
  cleanup concerns; neither changes SBE's narrow terminal-review bridge.
- 2026-09-03: Prototyped a terminal-review bridge for the Ganache assembly
  exception, then discarded it before review or release; no reconciliation,
  terminal-result, or API-ingress source change from that experiment remains.
- 2026-09-03: Owner clarification superseded the prior Slice 2 bridge before
  review or release: theme groups are dormant and must not participate in deck
  validity or handling. Reverted the uncommitted reconciliation/terminal-review
  experiment and its fixture. The actual correction is assembly-only: ignore
  legacy `ASSIGN THEME GROUPS.md` artifacts and omit unused theme-group output
  fields from the assembled deck. A complete multi-pass assembly with a
  deliberately malformed retained theme artifact passes provider-free.
- 2026-09-03: Extended the dormant-feature correction to future production
  split workspaces: pass six no longer asks the model to create the unused
  assignment artifact. The explicit builder flag remains available solely for
  historical and fixture compatibility.
- 2026-09-03: Focused provider-free assembly/workspace validation passed:
  51 tests across the v0.3 and dormant-policy modules, including the direct
  Ganache-shaped unregistered-chapter/workspace-generation witness. Slice 3
  source qualification is ready for API review; no wheel has been built.
- 2026-09-03: Slice 3 API review approved the clean-wheel gate. Built a clean
  0.4.42 candidate from archived tracked source plus only this correction;
  SHA-256 `5b18f4e25b12d92ca05ccd09e998a5f8051c69e1114a27a00ce5cbea35077de4`.
  Installed it without dependencies in an isolated release environment:
  `pip check` passed and the Ganache-shaped real-assembly witness ran from
  installed `site-packages`. Final release review remains required.
- 2026-09-03: Committed the source candidate as `72d5d6c` and pushed `main`.
  Final-package rebuilds use a minimal archive of that exact commit's
  `pyproject.toml` and package source, excluding unrelated dirty files and
  avoiding Windows long-path reference artifacts. With `SOURCE_DATE_EPOCH`
  pinned to the source commit timestamp, two builds were byte-identical:
  SHA-256 `58ccfaa319a244afea177e40e7c83e40b0e220c9bd86fe0034512ee19a28dc8d`.
  The final wheel installed cleanly, passed `pip check`, and passed the
  installed Ganache-shaped witness. Tag and publication remain intentionally
  pending final owner approval.
  `git diff --check` passed. No lifecycle, reconciliation, or terminal-result
  source is part of the retained correction.
