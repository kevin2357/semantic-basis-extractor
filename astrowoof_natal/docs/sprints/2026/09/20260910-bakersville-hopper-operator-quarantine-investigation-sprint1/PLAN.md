# Plan

## Status

Only Slice 0 is planned. No live quarantine execution, R2 access, provider
operation, runtime correction, process termination, or cleanup is authorized.

## Slice 0 — Reconstruct the nice assessment boundary

### 0A — Source and contract map

Trace the complete current path:

```text
genuine SBE workspace/checkpoint
  -> API archive restore
  -> installed astrowoof-operator-disposition-assessment
  -> stdout/stderr/exit/timeout
  -> API parser and identity validation
  -> quarantine dry-run admission or typed refusal
```

Record SBE custody classification inputs, CLI and package assumptions, API
restore/invocation behavior, stream handling, timeout, digest/identity joins,
and dry-run admission rules.

### 0B — Genuine provider-free fixture

Using supported SBE lifecycle APIs and writers—not hand-authored internal JSON—
create one synthetic workspace with a documented v1 `permitted` posture. Produce
and validate its exact checkpoint/archive and deterministic inventory with zero
provider operations or spend.

Stop if no supported construction path can produce that posture.

### 0C — Restore and installed subprocess proof

Restore the archive through API's real local restore boundary and invoke the
exact installed SBE CLI through API's production subprocess adapter from outside
both source trees. Prove restored byte equivalence, logical identity continuity,
canonical assessment output, exact digest/identity validation, and positive
dry-run admission without operational mutation.

### 0D — Failure matrix

Exercise controlled provider-free cases for permitted, prior-action, prohibited,
missing output, malformed/truncated output, wrong schema/digest/identity,
nonzero exit, timeout, incompatible installed package/resource, identity change,
and duplicate byte-identical read.

Classify each result at the SBE reader, SBE CLI, API invocation, API parser, or
API admission layer. A valid semantic refusal is successful transport, not an
assessment-process failure.

### 0E — Compare with Bakersville/Hopper evidence

When API supplies bounded, sanitized operator output, compare it only with the
proven deterministic classes. Do not infer equivalence from an outer run status
or a similar error phrase. Any retained workspace read requires separate exact
authorization.

## Exit gate

Publish one of these conclusions and pause for joint review:

- current nice path proven; historical cause unavailable;
- current deterministic defect reproduces the remembered symptom; or
- production boundary remains incomplete, with the missing prerequisite named.

The review decides whether to make a narrow correction, request exact retained
evidence, authorize a bounded live exercise, or return to the residual
not-so-nice suspension/hard-stop design.

