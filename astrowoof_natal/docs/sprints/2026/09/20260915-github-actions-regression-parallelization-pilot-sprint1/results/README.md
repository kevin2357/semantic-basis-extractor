# Results — GitHub Actions regression parallelization pilot

No hosted results exist yet.

Retain only compact, reviewable evidence here: summarized receipt identities,
digest comparisons, timing tables, run references, and final disposition. Raw
GitHub Actions artifacts remain attached to their immutable run unless a small
sanitized file is necessary for long-term interpretation.

Do not commit temporary work roots, pip caches, downloaded dependency artifacts,
credentials, provider data, authored workspaces, or full duplicated test logs.
