# Slice 1ε.2 — mutation and side-effect qualification

## Result

Complete and paused at Voof-paws 1ε. The executable source contract rejects 24
rehashed, structurally plausible lies using typed classifications, stable rule
IDs, and payload-safe detail codes.

## Mutation coverage

The campaign covers:

- terminal outcome versus final-custody/disposition eligibility;
- duplicate/changed ordinals, initial-pass identity, adjacency, and subject;
- adopted-output and next-input deck continuity;
- action-stage mismatch, incomplete/duplicate Response identity, and exact
  Response artifact/action/binding join;
- orphaned or population-invalid findings and cross-owned validation;
- unreachable selection and selected/delivered disagreement;
- changed artifact payload, forbidden prompt content, and packet-scoped IDs;
- omitted, altered, and reordered projections;
- falsified summaries, illegal source/event pairs, and forged envelope digests;
  and
- editorial request record overflow before downstream projection noise.

Each case begins with one of the valid package fixtures, mutates its target,
recomputes merely enclosing/self digests and deterministic request/gzip
expectations, then must fail on the deeper semantic rule.

## Qualification receipt

- Schema: `editorial_review_contract_qualification.v1`
- Receipt SHA-256:
  `3006564eb9daeafe2ba9064a10c8f415a4933702fe998be1bdc2319dafd0c406`
- Canonical size: 5,924 bytes
- Semantic manifest raw SHA-256:
  `ff6e7d5548edf8ff94be191064070f0ebe6407987f0a68d94416df250e007a20`
- Semantic manifest canonical SHA-256:
  `86b45106802fcea540d8ca0df39d69b5d9e6980193b2abfd2f91bbf01f5c9e7f`

The receipt contains identities, digests, typed outcomes, and counters only. It
contains no deck text, provider payload, prompt, credentials, endpoint tokens,
or API observation content.

## Side-effect boundary

The qualification is pure package-resource reading and in-memory evaluation.
Tests install hard failures for any socket or subprocess construction. The code
imports no provider, R2/storage, API/database, or retained-workspace adapter and
performs no writes. Its receipt records zero calls in every such class.

## Verification

```text
Ran 39 tests in 9.611s
OK (skipped=1)
```

This covers the contract foundation, positive fixtures, mutation/receipt
qualification, and suite-manifest integrity. The single skip remains the
optional third-party JSON Schema meta-validation; strict readers and semantic
validation are mandatory and passed.

## Voof-paws decision

Approved by API after an independent focused run (23 passed, one expected
optional-schema skip). The API-owned safe threshold is frozen at 9 MiB of
**compressed request bytes** beneath the verified 10 MiB compressed ceiling;
the atomic editorial request remains capped at 99 events. This approval clears
the separately fenced narrow runtime-builder slice, but does not authorize
Better Stack integration, retained-run access, installed-wheel work, or release.
