# Log

- 2026-09-02: Companion sprint created from frozen QA evidence.
- 2026-09-02: Revised the plan from the complete `✨🐶` trace. Recorded two
  distinct Waffle candidates: assembly retained a hard theme-distribution gate
  after pass acceptance made it advisory, and local-work consumption appears
  before finalization completion. Kept Scone as an independent typed-custody
  comparator. Owner review is sufficient to begin Slice 0; first API review is
  scheduled after production-path reproduction.
- 2026-09-03: Completed Slice 0. Confirmed the cross-layer policy contradiction:
  pass acceptance reports `theme_group_balance` as advisory, while final
  assembly still raises its legacy two-claim/2:1 `ValueError`. Reproduced the
  exact public ordering provider-free: completed provider evidence is adopted,
  that fan-in operation is consumed, then real final assembly fails without a
  typed result. Corrected the initial premature-consumption hypothesis: the
  consumed key belongs to fan-in/adoption, not final assembly. Structural
  unknown-theme rejection remains hard. Focused suite: 2 passed. Paused at
  Voof-paws 1.
- 2026-09-03: Incorporated API Voof-paws 1 approval. Completed Slice 1 by
  removing assembly's duplicate hard distribution checks while preserving its
  structural registry/action/section joins. The real Waffle-shaped public
  resume now assembles, validates, packages, publishes, and reaches
  `DELIVERY_COMPLETE`. A combined coverage/mirroring advisory fixture also
  assembles successfully; unknown registry values remain rejected. Focused
  evidence: 6 tests passed. No lifecycle, custody, provider, or result contract
  changed. Began Slice 2 analysis; typed deterministic failure remains gated at
  Voof-paws 2.
- 2026-09-03: Completed Slice 2 contract analysis without runtime mutation.
  Proposed a dedicated `AssemblyContractError` (remaining compatible as a
  `ValueError` subclass) and a sealed native-result v0.2 review cause
  `finalization_contract_invalid`. Operational subprocess/filesystem failures
  remain outside that catch and do not gain inferred semantic meaning. Paused
  at Voof-paws 2 because the new closed cause is API-consumed public semantics.
- 2026-09-03: Incorporated API Voof-paws 2 approval and completed Slices 2–3.
  Added the dedicated assembly-contract error type, closed v0.2 result cause,
  final-custody semantic condition, invocation-bound exit-2 envelope, and exact
  replay path. Operational failure remains untyped. Non-final provider or
  ambiguity custody prevents this disposition from sealing. Added interruption,
  privacy, API binding-join, contradictory rehash, and public-command tests.
  Focused sprint module: 7 passed; terminal-review contract suite: 9 passed with
  2 optional-schema skips. Proceeding to packaged qualification.
- 2026-09-03: Completed Slice 4. Added a packaged provider-free qualification
  command and closed schema. Source and isolated-wheel executions produced the
  same validated receipt (`8cc191…25d087`), covering advisory delivery,
  deterministic typed finalization review, exact replay, API action/binding
  joining, and operational-failure nonclassification. No provider, network,
  credentials, spend, R2, or retained QA workspace was used. Paused at
  Voof-paws 3 before any version bump or release preparation.
- 2026-09-03: Incorporated API Voof-paws 3 approval. No API schema relaxation
  or pre-release source patch is required; API will run its installed-artifact
  ingress regression after publication. Bumped the candidate to the fresh,
  unreleased `0.4.40` before beginning release-bound tests and builds.
- 2026-09-03: Completed Slice 5 release preparation. Seven focused runtime
  tests and twelve terminal/policy tests passed (two expected optional-schema
  skips). Two `0.4.40` wheels were byte-identical at `0a5904…54d8ad`; installed
  qualification (`dcbe51…948587`), package-surface inspection, dependency
  verification, and generic smoke passed. The full suite was intentionally not
  run and that omission is recorded in EVIDENCE. Awaiting explicit approval to
  commit, rebuild from committed source, tag, publish, and verify.
