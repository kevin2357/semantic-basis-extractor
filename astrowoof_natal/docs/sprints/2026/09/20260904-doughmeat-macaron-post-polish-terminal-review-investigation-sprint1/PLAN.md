# Plan

## Status

- Sprint complete; Slices 0-1 approved.
- The bounded artifact join confirms ordinary, evidence-backed editorial
  exhaustion in both runs. No runtime repair, API companion work, or release is
  indicated.
- API's final review accepted the result and publication joins without
  correction. Any future discussion of prompting, attempt budgets, or editorial
  thresholds is separate product-policy work, not retained-run recovery.

## Slice 0 — Frozen trace and terminal-outcome provenance

1. Parse the preserved worker-log chunks for each exact native run.
2. Build separate action/reconciliation/polish/finalization timelines.
3. Establish the exact native terminal reason and identify whether the traces
   disclose complete editorial issue-code evidence.
4. Record what is proven, what is merely suggested, and the smallest precise
   API-owned artifact packet needed if evidence remains incomplete.

No runtime changes, provider activity, retained-run mutation, or release work
belongs in Slice 0.

## Slice 1 — Exact terminal-artifact join

1. Freeze the API-supplied archive/result/receipt coordinates in local access
   manifests.
2. Perform exactly one conditional `HEAD` and one `GET` for each named archive.
3. Validate object identity, archive size/hash, safe member paths, and the exact
   result/receipt/checkpoint/snapshot joins.
4. Inspect only the named final validation, lint, polish-attempt, and terminal
   publication records needed to classify the outcomes.
5. Close the investigation if the evidence proves ordinary editorial exhaustion;
   otherwise propose only the narrowest evidence-supported follow-up.

## Later slices — not activated

Only after Slice 0, choose one evidence-supported path:

- document intended editorial terminal outcomes and close the investigation;
- make a narrow native validation/receipt correction with provider-free tests;
- or open a paired API sprint if an API-owned handoff or closeout defect is
  actually demonstrated.

None of those conditions was met.
