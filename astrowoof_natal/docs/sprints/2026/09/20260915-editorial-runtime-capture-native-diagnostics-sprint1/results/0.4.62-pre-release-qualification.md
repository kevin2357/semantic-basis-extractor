# 0.4.62 pre-release qualification

## Candidate identity

- Distribution: `astrowoof-natal-authoring`
- Version: `0.4.62`
- Artifact source: `4a17b44368d4e15280b73cbd78c3879d01796b5f`
- Fixed build epoch: `1789468315`
- Canonical wheel: `astrowoof_natal_authoring-0.4.62-py3-none-any.whl`
- Size: 1,385,622 bytes
- SHA-256: `eea9d74ec0ab39cc804ceedb4b00ce8af8cb17ce99c89d6b6276372ba00ab1bb`

## Source gates

- Focused release matrix: 43 passed.
- Complete manifest suite: 1,196 tests, three expected skips, zero failures.
- Test inventory SHA-256:
  `d85f093e8ebc4e98bbc4508eec4b9f2b5d5e3424b0e468a6c73d47df13110aab`.
- Full-suite wall time: 1,405.908561 seconds.
- New diagnostics test remains classified serial-only and logging-sensitive in
  the checked-in manifest.

The first focused invocation accidentally resolved API's installed SBE 0.4.61
before the source tests could prepend the checkout and therefore reported three
missing-diagnostics errors. The corrected command pre-imported the exact source
package and passed all 43 tests. This was import-topology contamination, not a
candidate failure.

## Build and package gates

- Two separate `git archive` exports of the artifact source were built with the
  same epoch.
- Wheel bytes and ordered 310-member inventories were identical.
- Forbidden tests, bytecode, and cache members: zero.
- Required runtime module, formatter, event catalog, and `py.typed`: present.
- Installed/source SHA-256 parity passed for the changed runtime and catalog.
- Candidate-prepended API-host dependency check: no broken requirements.

An abandoned initial build attempt incorrectly shared one extracted source tree
between concurrent builders and collided in setuptools' `build/` directory.
It produced no wheel. The two qualifying builds used independent clean exports.

## Installed gates

- Release smoke: passed from isolated `Lib/site-packages`.
- Lifecycle smoke: passed from isolated `Lib/site-packages`.
- Installed editorial runtime/diagnostics matrix: 15 passed, including ordinary
  delivery packet, review, typed unsupported, pre-assembly TypeError, and
  typed-status double fault.
- Real API-host coexistence: passed with the API's production initializer and
  SBE `force=False`; existing API handler preserved, exactly one SBE handler,
  two valid API stdout records, and 16 valid SBE records.
- Distinct failure phases:
  `pre_assembly_evidence_collection` and `typed_status_construction`.

The first release-smoke command used a target named `site`; its functional
checks completed but `--require-installed` correctly refused that path because
it was not named `site-packages`. The same exact wheel passed after installation
under an isolated `Lib/site-packages` path.

## Effects and remaining gate

- Provider operations: zero.
- Network, R2, Better Stack, and deployment operations: zero.
- Spend: zero.
- Authoritative workspace mutation: zero.
- Alloy impact: none; observation only.

Commit the release-lock record, rebuild twice from that exact commit at the
recorded epoch, and repeat installed qualification before API final pre-tag
review. Tag and publication remain unauthorized.
