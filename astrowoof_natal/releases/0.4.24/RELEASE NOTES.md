# AstroWoof Natal Authoring 0.4.24

Status: release-qualified; immutable publication pending

SBE 0.4.24 corrects the external-authority v2 payload-resolution boundary for
ordinary exact-Natal creative retries. New work persists one binding-owned private
complete-request artifact. The resolver follows only that snapshot-declared
reference and never recursively discovers candidate payloads.

For retained SBE 0.4.23 exact creative retries, the release provides one narrow,
fail-closed deterministic compatibility rebuild. It is available only after the
original immutable `request_payload_digest_mismatch` refusal and a fresh
inspection/request/grant decision. It binds runtime, resources, profile, provider
configuration, route, attempt, source, feedback, prompt, redaction shape, and final
request digest. Source archives outside the restored workspace are rejected.

The provider-free `astrowoof-payload-recovery-qa` command and closed qualification
receipt prove the complete refusal → fresh authority → one scripted create → exact
replay path. The command is qualification only and carries no execution authority.

## Qualification

- Artifact source commit: `00351b0d426d8aa0ea08635feae2c3b73db2d354`.
- Fixed build epoch: `1787659200`.
- Full source suite: 730 passed; 37 expected environment/opt-in skips.
- Two byte-identical wheels; SHA-256
  `eae1206e54e83e4f874de0595a8d2c616fc11a3980ba91f228d34e3186a27404`.
- Generic installed release smoke: pass with 50 cards and four summaries.
- Installed payload-recovery qualification: pass.
- Exact installed dependency: `semantic-projection-core==0.11.1`.
- External provider/network calls and spend: 0.
- Retained QA cohort access/mutation: 0.

The intended immutable tag is `astrowoof-natal-authoring-v0.4.24`.
