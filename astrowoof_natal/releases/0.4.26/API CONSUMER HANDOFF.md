# SBE 0.4.26 API Consumer Handoff

The new adversarial surfaces are qualification-only and provider-free. They do not
replace lifecycle inspection, native transition receipts, API leases, API capacity,
spend authority, reservations, publication authority, or PostgreSQL/R2 state.

Consumers should use the installed package's public readers and validators for:

- adversarial trace fixtures;
- route-matrix, systematic-explorer, and seeded-campaign receipts;
- the aggregate adversarial qualification receipt; and
- the closed 15-case consumer catalog.

The accepted joined campaign is recorded in AstroWoof API Sprint 52. Its corrected
receipt binds catalog SHA-256
`eea70ce9fed3c1ee986454dbac8e71e5e39b266f895628cd8adb2e53e9eab01e`
to 15 ordered discharges. Packaged fixtures retain their exact catalog hashes
separately from adapter-result hashes. The historical starvation shape and corrected
production-worker path carry distinct trace digests.

Do not reconstruct native meaning from `run.json`, prompts, packets, provider IDs,
or logs. Do not treat an adversarial qualification receipt as scheduling,
authorization, billing, or publication authority.

API pin/deployment requires its own compatibility and worker-image qualification;
publication of this wheel does not perform that adoption.
