# Compatibility — SBE 0.4.18

- Python: `>=3.11` (qualified on CPython 3.12 Windows).
- Direct dependency: `semantic-projection-core==0.11.0`.
- Lifecycle input: v0.5 inspection action relationships `blocking`,
  `independent`, and `superseded` project validly into lifecycle v0.6.
- No AGF, SPC, API database, Render configuration, or provider contract changes
  are required for this validation-only patch.
