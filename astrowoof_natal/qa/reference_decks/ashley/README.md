# Ashley Reference Decks

Ashley is represented as a female Poodle using `params.json`. The four versions
capture the transition from large multi-subject JSON authoring to independently
authored ten-card passes assembled into one deck.

The preferred pre-automation reference is `20260730-six-pass-manual-assembly`.
`selected-authoring-packet.json` is the current available QA companion packet,
not a claim that every historical version used that exact extractor output.
