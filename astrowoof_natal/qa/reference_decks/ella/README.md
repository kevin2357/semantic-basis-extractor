# Ella Reference Decks

Ella is represented as a female Miniature Schnauzer using `params.json`. Her
versions preserve the first real-service semantic-closure run and the later
Batch/cost-optimized recovery run.

`20260801-batch-optimized-final` is the preferred current automated reference.
`selected-authoring-packet.json` is the packet from that optimized Batch run.
