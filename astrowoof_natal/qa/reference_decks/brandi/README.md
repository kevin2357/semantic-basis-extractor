# Brandi Reference Decks

Brandi is represented as a female Schnauzer using `params.json`. The four
versions capture the same multi-subject and independent-pass experiments as
Ashley.

The preferred pre-automation reference is `20260730-six-pass-manual-assembly`.
`selected-authoring-packet.json` is the current available QA companion packet,
not a claim that every historical version used that exact extractor output.
